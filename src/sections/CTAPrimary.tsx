type SectionProps = { mode: string };

export default function CTAPrimary() {
  return (
    <section data-section="cta-primary">
      <header>
        <h2>CTAPrimary</h2>
      </header>
    </section>
  );
}
