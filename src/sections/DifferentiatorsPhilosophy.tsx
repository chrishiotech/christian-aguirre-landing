type SectionProps = { mode: string };

export default function DifferentiatorsPhilosophy({ mode }: SectionProps) {
  return (
    <section data-section="differentiators-philosophy" data-mode={mode}>
      <header>
        <h2>DifferentiatorsPhilosophy</h2>
      </header>
    </section>
  );
}
